# WebChat App

Technologies:

- WebSockets
- React

---

Catalog structure:

/build -> cloud package configuration (AWS)
/cmd -> główne pliki aplikacji (Golang)
/tools -> air - live reload dla Go
/web -> aplikacja React
